<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, input_html: { class: "border border-dark" } %>
  <%= f.input :rating, as: :integer, input_html: { min: 0, max: 5, step: 1 } %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
